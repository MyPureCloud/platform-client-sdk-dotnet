using System;
using System.Diagnostics;
using .Api;
using .Client;
using .Model;

namespace Example
{
    public class PutQualitySurveysScorableExample
    {
        public void main()
        { 

            var apiInstance = new PutQualitySurveysScorableRequest();
            var body = new ScorableSurvey(); // ScorableSurvey | survey
            var customerSurveyUrl = customerSurveyUrl_example;  // string | customerSurveyUrl

            try
            { 
                // Update a survey as an end-customer, for the purposes of scoring it.
                ScorableSurvey result = apiInstance.PutQualitySurveysScorable(body, customerSurveyUrl);
                Debug.WriteLine(result);
            }
            catch (Exception e)
            {
                Debug.Print("Exception when calling PutQualitySurveysScorableRequest.PutQualitySurveysScorable: " + e.Message );
            }
        }
    }
}